let body;
/*
function blur() {
    body = selector("body");
    let blur = 5;
    let id = setInterval(frame, 10);
    function frame() {
        if (blur === 0) {
            clearInterval(id);
        } else {
            blur -= 0.1;
            body.style.filter = "blur(" + blur + "px)";
        }
    }
}
*/